spring.application.name=ms-lending-app-product-service
server.port=8080

# R2DBC PostgreSQL
spring.r2dbc.url=r2dbc:postgresql://localhost:5432/lending_product_db
spring.r2dbc.username=postgres
spring.r2dbc.password=postgres

# Flyway Database Migrations (runs over JDBC at startup, then R2DBC takes over)
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# JDBC datasource (used exclusively by Flyway for migrations)
spring.datasource.url=jdbc:postgresql://localhost:5432/lending_product_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Kafka
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# API Key Security
app.security.api-key=product-service-api-key-2024

# Spring Cache (ConcurrentMapCacheManager)
spring.cache.type=simple
spring.cache.cache-names=products,productFees,productTenures

# Logging
logging.level.com.glo.lending.product=DEBUG
logging.level.org.springframework.r2dbc=DEBUG

